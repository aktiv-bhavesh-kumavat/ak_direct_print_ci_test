# AK Direct Print — macOS CI test

Sandbox repo for validating the AK Direct Print macOS agent build on real
Apple Silicon hardware and current macOS versions via GitHub Actions hosted
runners, since no such hardware/OS is available locally.

Not the production module — just the `agent/` folder needed to build and
smoke-test the macOS agent. See `.github/workflows/build-macos.yml`.
